{
  "name": "fullstack_todo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd backend && npm install) || (cd frontend && npm install)",
    "backend": "node ./backend/server.js",
    "frontend": "cd frontend && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
